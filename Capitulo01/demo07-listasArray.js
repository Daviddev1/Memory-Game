const minhaLista = []
const minhaListaDeTarefa = [
    'mandar email',
    'colocar comida',
    'limpar o quarto'
]

    console.log(minhaListaDeTarefa[0])
    console.log(minhaListaDeTarefa[1])
    console.log(minhaListaDeTarefa[4])

    //quantidade de itens no Array
    //console.log(minhaListaDeTarefa.length)

    //Adicionar item (PUSH)
    minhaListaDeTarefa.push('lavar carro')
   // console.log(minhaListaDeTarefa);

    //remover ultimo item da lista (POP)
    //const ultimo = minhaListaDeTarefa.pop()
    //console.log(ultimo, minhaListaDeTarefa)

    //remover primeiro item da lista (SHIFT)
    //const primeiro = minhaListaDeTarefa.shift()
        //console.log(primeiro, minhaListaDeTarefa)

    // remover item especifico da lista a partir de um indice
        //console.log(minhaListaDeTarefa[2])
        
        // qual item de inicio(onde está o item)(SLICE)
        // quantos itens remover
      // minhaListaDeTarefa.splice(2, 1)
       //console.log(minhaListaDeTarefa)

const itens = [ 1, 'PC', 0.22 ]
  
// Verificar o tipo do array
      // console.log(typeof(itens) )
    // Verificar se é  array da  forma correta
       // console.log(Array.isArray(itens))  
 
 // ORDENAR uma lista  (SORT)
    const numeros = [3, 2, 4, 1, 0]
    const letras = ['d','a','v','i']
   // console.log(numeros.sort())  
   // console.log(letras.sort())

 // JUNTAR dois arrays (CONCAT)
    //const novo = itens.concat([1,2,3])   
    //console.log(novo)

 // JUNTAR arrays em uma só STRING (.JOIN)
    //console.log(itens.join(', '))

  // verificar indice do item no array (.INDEXOF)
    const index = itens.indexOf('PC')
    //console.log(index) // mostrar pra mim em qual posiçao ele está na lista
    //console.log(itens[index]) // mostra pra mim o item especifico na lista







        


  
